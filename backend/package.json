{
  "name": "gig-hunter-backend",
  "version": "2.0.0",
  "description": "Gig Hunter MVP — Phase A Backend (Express + Sequelize + OpenAI)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:reset": "node -e \"require('./config/database').sequelize.sync({ force: true }).then(() => { console.log('DB reset'); process.exit(0); })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "openai": "^4.47.1",
    "rss-parser": "^3.13.0",
    "sequelize": "^6.37.3",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
